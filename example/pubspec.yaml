name: easy_notifications_example
description: Example app for the Easy Notifications package

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  cupertino_icons: ^1.0.2
  easy_notifications: { path: ../ }
  flutter: { sdk: flutter }

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/hamster_on_hands.jpg
